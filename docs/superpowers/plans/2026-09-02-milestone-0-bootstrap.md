# ReVoxMobile Milestone 0: Bootstrap

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A buildable iOS project skeleton for ReVox Mobile: an XcodeGen `project.yml` with three targets, a Linux-testable `ReVoxCore` package, a placeholder icon, privacy manifest, entitlements, Info.plists, a CI workflow that proves the app and extension build and test on an iPhone simulator, a TestFlight workflow that is ready the moment the Apple secrets exist, and the owner-facing docs.

**Architecture:** One XcodeGen-generated Xcode project (`ReVoxMobile.xcodeproj`, never committed) with targets `ReVoxMobile` (SwiftUI app), `ReVoxBroadcast` (ReplayKit Broadcast Upload Extension) and `ReVoxMobileTests` (XCTest bundle hosted by the app), plus a local Swift package `ReVoxCore` (Foundation only, `swift test` runs on Linux). Third-party packages pinned exactly: WhisperKit 1.1.0 (package `argmax-oss-swift`, product `WhisperKit`) and FluidAudio 0.15.6 (product `FluidAudio`).

**Tech Stack:** Swift 5 language mode, iOS 17.0 deployment target, SwiftUI, XcodeGen, GitHub Actions (`ubuntu-24.04` with the `swift:6.3-noble` container for the package; `macos-26` with Xcode 26.6 for the app), App Store Connect API key signing.

## Global Constraints

- Work from the repository root `/home/user/ReVoxMobile` on branch `milestone/0-bootstrap`. Never commit a generated `.xcodeproj`, `.spm/`, `build/`, or any key/certificate file.
- Local Swift toolchain: `export PATH=/home/user/swift/usr/bin:$PATH` (Swift 6.3.3, Linux x86_64). Only `ReVoxCore` can be compiled here. App and extension targets compile only in CI on macOS; implementers touching them must say so in their report and must not claim they build.
- `ReVoxCore` imports only `Foundation` (and `XCTest` in tests). No UIKit, AVFoundation, CoreML, SwiftUI, Combine, or Darwin-only imports. `swift-tools-version: 5.10`, Swift 5 language mode, XCTest (not swift-testing) so the same tests run on Linux and in the simulator.
- Exact names, used everywhere (project.yml, workflows, docs):
  - Xcode project: `ReVoxMobile.xcodeproj`; scheme: `ReVoxMobile`.
  - Targets: `ReVoxMobile` (application), `ReVoxBroadcast` (app-extension, `com.apple.broadcast-services-upload`), `ReVoxMobileTests` (bundle.unit-test).
  - Local package: `ReVoxCore` at `ReVoxCore/` (product `ReVoxCore`).
  - Bundle ids come from ONE build setting `REVOX_BUNDLE_PREFIX` (default `com.mchrisgm`, overridable on the `xcodebuild` command line and via the repository variable `REVOX_BUNDLE_PREFIX`): app `$(REVOX_BUNDLE_PREFIX).revox`, extension `$(REVOX_BUNDLE_PREFIX).revox.broadcast`, tests `$(REVOX_BUNDLE_PREFIX).revox.tests`, App Group `group.$(REVOX_BUNDLE_PREFIX).revox`.
  - Build settings: `IPHONEOS_DEPLOYMENT_TARGET 17.0`, `TARGETED_DEVICE_FAMILY 1`, `SWIFT_VERSION 5.0`, `MARKETING_VERSION 0.1.0`, `CURRENT_PROJECT_VERSION 1` (CI overrides with `github.run_number`), `CODE_SIGN_STYLE Automatic`.
  - Secrets read by `testflight.yml`, exact names: `ASC_KEY_ID`, `ASC_ISSUER_ID`, `ASC_KEY_P8` (base64 of the `.p8`), `APPLE_TEAM_ID`; fallback path: `DIST_CERT_P12` (base64), `DIST_CERT_PASSWORD`, `PROFILE_APP` (base64 `.mobileprovision`), `PROFILE_EXTENSION` (base64 `.mobileprovision`).
  - Runner pins: Linux job `ubuntu-24.04` + `container: swift:6.3-noble`; macOS jobs `macos-26` with `XCODE_VERSION: "26.6"` selected by `scripts/ci/select-xcode.sh` (falls back to the newest Xcode 26.x on the image with a `::warning::`).
- Info.plist keys required by the platform: `NSMicrophoneUsageDescription`, `UIBackgroundModes: [audio]`, `ITSAppUsesNonExemptEncryption: false` in BOTH the app and the extension plist, `REVOXAppGroup` and `REVOXBroadcastExtensionBundleID` in the app plist (so code reads the ids from one place).
- Privacy manifest `PrivacyInfo.xcprivacy` in both targets declaring: UserDefaults (`CA92.1`), FileTimestamp (`C617.1`), DiskSpace (`E174.1`, `85F4.1`); `NSPrivacyTracking false`; empty `NSPrivacyCollectedDataTypes` and `NSPrivacyTrackingDomains`.
- App icon: a single 1024×1024 RGB PNG without alpha at `ReVoxMobile/Assets.xcassets/AppIcon.appiconset/AppIcon.png`, generated by `scripts/make_icon.py` (Python 3 standard library only) and committed.
- Every commit message ends with the two trailer lines:
  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_017rNMt2Xns5wUvUYmLHyjKR
  ```
  No model identifiers anywhere else (code, comments, docs, plists, workflow files).
- Every GitHub-facing document is written for the repository owner and a non-technical tester; British or American spelling is fine but be consistent within a file.

## File Structure

```
project.yml                         XcodeGen spec (three targets, packages, scheme)
ReVoxCore/
  Package.swift
  Sources/ReVoxCore/ReVoxCore.swift  namespace + sampleRate constant
  Tests/ReVoxCoreTests/ReVoxCoreTests.swift
ReVoxMobile/
  App/ReVoxMobileApp.swift          @main SwiftUI app
  App/ContentView.swift             placeholder screen
  App/LinkedDependencies.swift      imports WhisperKit + FluidAudio + ReVoxCore so CI proves they resolve and compile
  Info.plist
  ReVoxMobile.entitlements          App Group
  PrivacyInfo.xcprivacy
  Assets.xcassets/Contents.json
  Assets.xcassets/AppIcon.appiconset/Contents.json
  Assets.xcassets/AppIcon.appiconset/AppIcon.png
  Assets.xcassets/AccentColor.colorset/Contents.json
ReVoxMobileTests/
  SmokeTests.swift                  XCTest that imports ReVoxCore and the app module
ReVoxBroadcast/
  SampleHandler.swift               RPBroadcastSampleHandler skeleton
  Info.plist
  ReVoxBroadcast.entitlements       App Group
  PrivacyInfo.xcprivacy
ci/ExportOptions.plist              app-store-connect export template (teamID placeholder)
scripts/make_icon.py
scripts/ci/select-xcode.sh
scripts/ci/pick-simulator.sh
.github/workflows/ci.yml
.github/workflows/testflight.yml
README.md
docs/release.md
docs/testing.md
```

---

### Task 1: ReVoxCore package with one passing test

**Files:**
- Create: `ReVoxCore/Package.swift`, `ReVoxCore/Sources/ReVoxCore/ReVoxCore.swift`, `ReVoxCore/Tests/ReVoxCoreTests/ReVoxCoreTests.swift`

**Interfaces:**
- Produces: `public enum ReVoxCore { public static let sampleRate: Int }` = `16_000`. Later milestones add every platform-independent type to this package.

- [ ] **Step 1: Write the failing test**

`ReVoxCore/Tests/ReVoxCoreTests/ReVoxCoreTests.swift`:
```swift
import XCTest
@testable import ReVoxCore

final class ReVoxCoreTests: XCTestCase {
    func testPipelineSampleRateIs16kHz() {
        XCTAssertEqual(ReVoxCore.sampleRate, 16_000)
    }
}
```

`ReVoxCore/Package.swift`:
```swift
// swift-tools-version: 5.10
import PackageDescription

let package = Package(
    name: "ReVoxCore",
    platforms: [.iOS(.v17), .macOS(.v14)],
    products: [
        .library(name: "ReVoxCore", targets: ["ReVoxCore"]),
    ],
    targets: [
        .target(name: "ReVoxCore"),
        .testTarget(name: "ReVoxCoreTests", dependencies: ["ReVoxCore"]),
    ]
)
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `cd /home/user/ReVoxMobile/ReVoxCore && export PATH=/home/user/swift/usr/bin:$PATH && swift test`
Expected: compile error, `ReVoxCore` has no member `sampleRate` (or the target has no sources).

- [ ] **Step 3: Implement**

`ReVoxCore/Sources/ReVoxCore/ReVoxCore.swift`:
```swift
import Foundation

/// Platform-independent logic shared by the ReVox iOS app and its broadcast extension.
///
/// Everything in this module depends on Foundation only, so `swift test` runs on Linux
/// as well as inside the iOS simulator.
public enum ReVoxCore {
    /// Sample rate, in Hz, that every pipeline stage works at.
    public static let sampleRate = 16_000
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `cd /home/user/ReVoxMobile/ReVoxCore && export PATH=/home/user/swift/usr/bin:$PATH && swift test`
Expected: `Executed 1 test, with 0 failures`. Output must be free of warnings.

- [ ] **Step 5: Commit**

```bash
cd /home/user/ReVoxMobile
git add ReVoxCore
git commit -m "feat(core): ReVoxCore package skeleton with sample-rate constant"   # plus the mandatory trailers
```

---

### Task 2: XcodeGen project, app, extension and test targets, icon, plists, entitlements, privacy manifests

**Files:**
- Create: `project.yml`, everything under `ReVoxMobile/`, `ReVoxMobileTests/`, `ReVoxBroadcast/`, `ci/ExportOptions.plist`, `scripts/make_icon.py`
- Modify: `.gitignore` (add `.spm/` and `ci/ExportOptions.generated.plist`)

**Interfaces:**
- Consumes: `ReVoxCore` package from Task 1.
- Produces: `xcodegen generate` yields `ReVoxMobile.xcodeproj` with scheme `ReVoxMobile` whose test action runs `ReVoxMobileTests`; app embeds `ReVoxBroadcast`; both link `ReVoxCore`; the app links `WhisperKit` and `FluidAudio`.

You cannot run `xcodegen` or `xcodebuild` on this Linux machine. If `/home/user/xcodegen-src/.build/release/xcodegen` exists, run `/home/user/xcodegen-src/.build/release/xcodegen generate --spec project.yml` from the repository root to validate the spec (then `rm -rf ReVoxMobile.xcodeproj`); if it does not exist, validate the YAML with `python3 -c "import yaml,sys; yaml.safe_load(open('project.yml'))"` (install PyYAML with `pip3 install --user pyyaml` if missing) and say in your report that the project was not generated locally. Validate every `.plist`/`.xcprivacy`/`.entitlements` with `python3 -c "import plistlib,sys; plistlib.load(open(sys.argv[1],'rb'))" FILE`.

- [ ] **Step 1: `project.yml`** (write exactly this):

```yaml
name: ReVoxMobile
options:
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  generateEmptyDirectories: false
  groupSortPosition: top

settings:
  base:
    # Single source of truth for every bundle identifier and the App Group.
    # Override on the command line (REVOX_BUNDLE_PREFIX=com.example) or in CI.
    REVOX_BUNDLE_PREFIX: com.mchrisgm
    # Provisioning profile names, only used by the manual signing path in testflight.yml.
    REVOX_PROFILE_APP: ""
    REVOX_PROFILE_EXTENSION: ""
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"
    TARGETED_DEVICE_FAMILY: "1"
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_STYLE: Automatic
    GENERATE_INFOPLIST_FILE: NO
    ENABLE_USER_SCRIPT_SANDBOXING: YES

packages:
  ReVoxCore:
    path: ReVoxCore
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit.git
    exactVersion: 1.1.0
  FluidAudio:
    url: https://github.com/FluidInference/FluidAudio.git
    exactVersion: 0.15.6

targets:
  ReVoxMobile:
    type: application
    platform: iOS
    sources:
      - path: ReVoxMobile
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(REVOX_BUNDLE_PREFIX).revox
        PRODUCT_NAME: ReVoxMobile
        INFOPLIST_FILE: ReVoxMobile/Info.plist
        CODE_SIGN_ENTITLEMENTS: ReVoxMobile/ReVoxMobile.entitlements
        PROVISIONING_PROFILE_SPECIFIER: $(REVOX_PROFILE_APP)
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
    dependencies:
      - target: ReVoxBroadcast
      - package: ReVoxCore
      - package: WhisperKit
        product: WhisperKit
      - package: FluidAudio
        product: FluidAudio
    scheme:
      testTargets:
        - ReVoxMobileTests
      gatherCoverageData: false

  ReVoxBroadcast:
    type: app-extension
    platform: iOS
    sources:
      - path: ReVoxBroadcast
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(REVOX_BUNDLE_PREFIX).revox.broadcast
        PRODUCT_NAME: ReVoxBroadcast
        INFOPLIST_FILE: ReVoxBroadcast/Info.plist
        CODE_SIGN_ENTITLEMENTS: ReVoxBroadcast/ReVoxBroadcast.entitlements
        PROVISIONING_PROFILE_SPECIFIER: $(REVOX_PROFILE_EXTENSION)
        APPLICATION_EXTENSION_API_ONLY: YES
    dependencies:
      - package: ReVoxCore

  ReVoxMobileTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: ReVoxMobileTests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(REVOX_BUNDLE_PREFIX).revox.tests
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: ReVoxMobile
      - package: ReVoxCore
```

- [ ] **Step 2: App sources**

`ReVoxMobile/App/ReVoxMobileApp.swift`:
```swift
import SwiftUI

@main
struct ReVoxMobileApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

`ReVoxMobile/App/ContentView.swift`:
```swift
import SwiftUI
import ReVoxCore

/// Placeholder root screen. Milestone 3 replaces it with the real Main screen.
struct ContentView: View {
    var body: some View {
        NavigationStack {
            ContentUnavailableView(
                "ReVox Mobile",
                systemImage: "waveform.and.mic",
                description: Text("Live speech-to-English translation, entirely on this iPhone.\nPipeline sample rate: \(ReVoxCore.sampleRate) Hz")
            )
            .navigationTitle("ReVox")
        }
    }
}

#Preview {
    ContentView()
}
```

`ReVoxMobile/App/LinkedDependencies.swift`:
```swift
// Importing the third-party modules here makes the CI build prove that the pinned
// WhisperKit and FluidAudio versions resolve and compile for the iOS simulator,
// before any milestone depends on their APIs.
import FluidAudio
import ReVoxCore
import WhisperKit

enum LinkedDependencies {
    static let whisperKitVersion = "1.1.0"
    static let fluidAudioVersion = "0.15.6"
}
```

- [ ] **Step 3: App `Info.plist`, entitlements, privacy manifest, asset catalog**

`ReVoxMobile/Info.plist` (XML plist 1.0) with exactly these keys:
| Key | Value |
|---|---|
| CFBundleDevelopmentRegion | `$(DEVELOPMENT_LANGUAGE)` |
| CFBundleDisplayName | `ReVox` |
| CFBundleExecutable | `$(EXECUTABLE_NAME)` |
| CFBundleIdentifier | `$(PRODUCT_BUNDLE_IDENTIFIER)` |
| CFBundleInfoDictionaryVersion | `6.0` |
| CFBundleName | `$(PRODUCT_NAME)` |
| CFBundlePackageType | `$(PRODUCT_BUNDLE_PACKAGE_TYPE)` |
| CFBundleShortVersionString | `$(MARKETING_VERSION)` |
| CFBundleVersion | `$(CURRENT_PROJECT_VERSION)` |
| ITSAppUsesNonExemptEncryption | false |
| LSRequiresIPhoneOS | true |
| NSMicrophoneUsageDescription | `ReVox listens to speech so it can translate it to English on this iPhone. Audio never leaves the device.` |
| REVOXAppGroup | `group.$(REVOX_BUNDLE_PREFIX).revox` |
| REVOXBroadcastExtensionBundleID | `$(REVOX_BUNDLE_PREFIX).revox.broadcast` |
| UIBackgroundModes | array: `audio` |
| UILaunchScreen | empty dict |
| UISupportedInterfaceOrientations | array: `UIInterfaceOrientationPortrait` |

`ReVoxMobile/ReVoxMobile.entitlements`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.application-groups</key>
	<array>
		<string>group.$(REVOX_BUNDLE_PREFIX).revox</string>
	</array>
</dict>
</plist>
```

`ReVoxMobile/PrivacyInfo.xcprivacy` (same content is reused verbatim for the extension):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSPrivacyTracking</key>
	<false/>
	<key>NSPrivacyTrackingDomains</key>
	<array/>
	<key>NSPrivacyCollectedDataTypes</key>
	<array/>
	<key>NSPrivacyAccessedAPITypes</key>
	<array>
		<dict>
			<key>NSPrivacyAccessedAPIType</key>
			<string>NSPrivacyAccessedAPICategoryUserDefaults</string>
			<key>NSPrivacyAccessedAPITypeReasons</key>
			<array>
				<string>CA92.1</string>
			</array>
		</dict>
		<dict>
			<key>NSPrivacyAccessedAPIType</key>
			<string>NSPrivacyAccessedAPICategoryFileTimestamp</string>
			<key>NSPrivacyAccessedAPITypeReasons</key>
			<array>
				<string>C617.1</string>
			</array>
		</dict>
		<dict>
			<key>NSPrivacyAccessedAPIType</key>
			<string>NSPrivacyAccessedAPICategoryDiskSpace</string>
			<key>NSPrivacyAccessedAPITypeReasons</key>
			<array>
				<string>E174.1</string>
				<string>85F4.1</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
```

`ReVoxMobile/Assets.xcassets/Contents.json`:
```json
{
  "info" : {
    "author" : "xcode",
    "version" : 1
  }
}
```

`ReVoxMobile/Assets.xcassets/AppIcon.appiconset/Contents.json`:
```json
{
  "images" : [
    {
      "filename" : "AppIcon.png",
      "idiom" : "universal",
      "platform" : "ios",
      "size" : "1024x1024"
    }
  ],
  "info" : {
    "author" : "xcode",
    "version" : 1
  }
}
```

`ReVoxMobile/Assets.xcassets/AccentColor.colorset/Contents.json`:
```json
{
  "colors" : [
    {
      "color" : {
        "color-space" : "srgb",
        "components" : {
          "alpha" : "1.000",
          "blue" : "0.549",
          "green" : "0.471",
          "red" : "0.071"
        }
      },
      "idiom" : "universal"
    }
  ],
  "info" : {
    "author" : "xcode",
    "version" : 1
  }
}
```

- [ ] **Step 4: Icon generator** `scripts/make_icon.py` (write exactly this, then run `python3 scripts/make_icon.py` and commit the PNG it writes):

```python
#!/usr/bin/env python3
"""Generate the placeholder ReVox app icon as a 1024x1024 RGB PNG (no alpha).

Standard library only, so it runs anywhere. App Store icons must not contain
an alpha channel, hence colour type 2 (RGB).
"""

from __future__ import annotations

import math
import pathlib
import struct
import zlib

SIZE = 1024
OUTPUT = pathlib.Path(__file__).resolve().parents[1] / (
    "ReVoxMobile/Assets.xcassets/AppIcon.appiconset/AppIcon.png"
)

TOP = (14, 32, 92)  # deep navy
BOTTOM = (18, 120, 140)  # teal
INK = (255, 255, 255)


def _chunk(tag: bytes, payload: bytes) -> bytes:
    crc = zlib.crc32(tag + payload) & 0xFFFFFFFF
    return struct.pack(">I", len(payload)) + tag + payload + struct.pack(">I", crc)


def _pixel(x: int, y: int) -> tuple[int, int, int]:
    t = y / (SIZE - 1)
    background = tuple(round(a + (b - a) * t) for a, b in zip(TOP, BOTTOM))

    cx, cy = SIZE * 0.36, SIZE * 0.5
    dx, dy = x - cx, y - cy
    r = math.hypot(dx, dy)

    if r < SIZE * 0.12:  # the "voice" dot
        return INK
    if dx > 0 and abs(dy) < dx * 1.1:  # three arcs fanning out to the right
        for radius in (0.24, 0.35, 0.46):
            if abs(r - SIZE * radius) < SIZE * 0.03:
                return INK
    return background  # type: ignore[return-value]


def render() -> bytes:
    rows = bytearray()
    for y in range(SIZE):
        rows.append(0)  # PNG filter type: none
        for x in range(SIZE):
            rows.extend(_pixel(x, y))
    header = struct.pack(">IIBBBBB", SIZE, SIZE, 8, 2, 0, 0, 0)
    return (
        b"\x89PNG\r\n\x1a\n"
        + _chunk(b"IHDR", header)
        + _chunk(b"IDAT", zlib.compress(bytes(rows), 9))
        + _chunk(b"IEND", b"")
    )


if __name__ == "__main__":
    OUTPUT.parent.mkdir(parents=True, exist_ok=True)
    OUTPUT.write_bytes(render())
    print(f"wrote {OUTPUT} ({OUTPUT.stat().st_size} bytes)")
```

Verify the PNG header after generating: `python3 -c "import struct;d=open('ReVoxMobile/Assets.xcassets/AppIcon.appiconset/AppIcon.png','rb').read(33);print(struct.unpack('>IIBB',d[16:26]))"` must print `(1024, 1024, 8, 2)`.

- [ ] **Step 5: Test target** `ReVoxMobileTests/SmokeTests.swift`:
```swift
import XCTest
import ReVoxCore
@testable import ReVoxMobile

final class SmokeTests: XCTestCase {
    func testCoreIsLinkedIntoTheApp() {
        XCTAssertEqual(ReVoxCore.sampleRate, 16_000)
    }

    func testPinnedDependencyVersionsAreRecorded() {
        XCTAssertEqual(LinkedDependencies.whisperKitVersion, "1.1.0")
        XCTAssertEqual(LinkedDependencies.fluidAudioVersion, "0.15.6")
    }
}
```

- [ ] **Step 6: Broadcast extension**

`ReVoxBroadcast/SampleHandler.swift`:
```swift
import ReplayKit

/// Receives audio from other apps while a system broadcast is running.
///
/// Milestone 0 only proves the target builds and embeds. Milestone 5 converts the
/// `.audioApp` buffers to mono 16 kHz and writes them into the App Group ring buffer.
final class SampleHandler: RPBroadcastSampleHandler {
    override func broadcastStarted(withSetupInfo setupInfo: [String: NSObject]?) {
        super.broadcastStarted(withSetupInfo: setupInfo)
    }

    override func processSampleBuffer(_ sampleBuffer: CMSampleBuffer, with sampleBufferType: RPSampleBufferType) {
        guard sampleBufferType == .audioApp else { return }
        // Intentionally empty until milestone 5.
    }

    override func broadcastFinished() {
        super.broadcastFinished()
    }
}
```

`ReVoxBroadcast/Info.plist` keys:
| Key | Value |
|---|---|
| CFBundleDevelopmentRegion | `$(DEVELOPMENT_LANGUAGE)` |
| CFBundleDisplayName | `ReVox` |
| CFBundleExecutable | `$(EXECUTABLE_NAME)` |
| CFBundleIdentifier | `$(PRODUCT_BUNDLE_IDENTIFIER)` |
| CFBundleInfoDictionaryVersion | `6.0` |
| CFBundleName | `$(PRODUCT_NAME)` |
| CFBundlePackageType | `$(PRODUCT_BUNDLE_PACKAGE_TYPE)` |
| CFBundleShortVersionString | `$(MARKETING_VERSION)` |
| CFBundleVersion | `$(CURRENT_PROJECT_VERSION)` |
| ITSAppUsesNonExemptEncryption | false |
| REVOXAppGroup | `group.$(REVOX_BUNDLE_PREFIX).revox` |
| NSExtension | dict: `NSExtensionPointIdentifier` = `com.apple.broadcast-services-upload`, `NSExtensionPrincipalClass` = `$(PRODUCT_MODULE_NAME).SampleHandler`, `RPBroadcastProcessMode` = `RPBroadcastProcessModeSampleBuffer` |

`ReVoxBroadcast/ReVoxBroadcast.entitlements`: identical to the app entitlements (same App Group string).
`ReVoxBroadcast/PrivacyInfo.xcprivacy`: identical to the app privacy manifest.

- [ ] **Step 7: Export options template** `ci/ExportOptions.plist`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<key>destination</key>
	<string>export</string>
	<key>signingStyle</key>
	<string>automatic</string>
	<key>teamID</key>
	<string>__TEAM_ID__</string>
	<key>uploadSymbols</key>
	<true/>
	<key>manageAppVersionAndBuildNumber</key>
	<false/>
</dict>
</plist>
```

- [ ] **Step 8: `.gitignore` additions**: append `.spm/` and `ci/ExportOptions.generated.plist` (keep the existing entries).

- [ ] **Step 9: Validate and commit**

Validate every plist-format file with `plistlib`, the YAML with PyYAML (or the Linux-built xcodegen if present), the PNG header, then:
```bash
git add project.yml ReVoxMobile ReVoxMobileTests ReVoxBroadcast ci scripts/make_icon.py .gitignore
git commit -m "feat: XcodeGen project with app, broadcast extension and test targets"   # plus the mandatory trailers
```

---

### Task 3: CI and TestFlight workflows

**Files:**
- Create: `.github/workflows/ci.yml`, `.github/workflows/testflight.yml`, `scripts/ci/select-xcode.sh`, `scripts/ci/pick-simulator.sh` (both executable, `chmod +x`)

**Interfaces:**
- Consumes: scheme `ReVoxMobile`, project `ReVoxMobile.xcodeproj` (generated), `ci/ExportOptions.plist`, build settings `REVOX_BUNDLE_PREFIX`, `REVOX_PROFILE_APP`, `REVOX_PROFILE_EXTENSION` from Task 2.
- Produces: `ci.yml` (jobs `core-linux`, `ios-simulator`) and `testflight.yml` (jobs `preflight`, `upload`).

Validate both YAML files with PyYAML and both scripts with `bash -n`. Expressions like `${{ steps.x.outputs.y }}` and secrets must be passed to `run:` steps through `env:` (never interpolated directly into the script body).

- [ ] **Step 1: `scripts/ci/select-xcode.sh`**
```bash
#!/usr/bin/env bash
# Select the pinned Xcode on a GitHub macOS runner, falling back to the newest
# Xcode with the same major version if the image no longer ships the pinned one.
# Usage: scripts/ci/select-xcode.sh 26.6
set -euo pipefail

want=${1:?usage: select-xcode.sh <version, e.g. 26.6>}
app="/Applications/Xcode_${want}.app"

if [ ! -d "$app" ]; then
  major=${want%%.*}
  echo "::warning::Xcode ${want} is not on this runner image; falling back to the newest Xcode ${major}.x"
  app=$(ls -d /Applications/Xcode_"${major}".*.app 2>/dev/null | sort -V | tail -n 1 || true)
  if [ -z "$app" ]; then
    echo "::error::No Xcode ${major}.x found. Installed:"
    ls /Applications | grep -i xcode || true
    exit 1
  fi
fi

sudo xcode-select -s "$app"
echo "Selected $app"
xcodebuild -version
```

- [ ] **Step 2: `scripts/ci/pick-simulator.sh`**
```bash
#!/usr/bin/env bash
# Print "destination=platform=iOS Simulator,id=<udid>" for an available iPhone
# simulator on the newest installed iOS runtime, suitable for $GITHUB_OUTPUT.
set -euo pipefail

devices=$(xcrun simctl list devices available -j)
udid=$(printf '%s' "$devices" | jq -r '
  .devices
  | to_entries
  | map(select(.key | test("SimRuntime\\.iOS-")))
  | sort_by(.key | capture("iOS-(?<major>[0-9]+)-(?<minor>[0-9]+)") | [(.major | tonumber), (.minor | tonumber)])
  | reverse
  | map(.value[] | select(.name | test("^iPhone")))
  | .[0].udid // empty')

if [ -z "$udid" ]; then
  echo "::error::No available iPhone simulator found" >&2
  xcrun simctl list devices available >&2
  exit 1
fi

name=$(printf '%s' "$devices" | jq -r --arg u "$udid" '.devices[][] | select(.udid == $u) | .name')
echo "Using simulator: $name ($udid)" >&2
echo "destination=platform=iOS Simulator,id=$udid"
```

- [ ] **Step 3: `.github/workflows/ci.yml`**
```yaml
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  XCODE_VERSION: "26.6"

jobs:
  core-linux:
    name: ReVoxCore tests (Linux)
    runs-on: ubuntu-24.04
    container: swift:6.3-noble
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: swift test
        working-directory: ReVoxCore
        run: swift test

  ios-simulator:
    name: App and extension (iOS simulator)
    runs-on: macos-26
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: scripts/ci/select-xcode.sh "$XCODE_VERSION"

      - name: Install XcodeGen and xcbeautify
        run: brew install xcodegen xcbeautify

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Cache Swift package checkouts
        uses: actions/cache@v4
        with:
          path: .spm
          key: spm-${{ runner.os }}-${{ hashFiles('project.yml') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Resolve Swift packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -clonedSourcePackagesDirPath .spm

      - name: Pick simulator
        id: sim
        run: scripts/ci/pick-simulator.sh >> "$GITHUB_OUTPUT"

      - name: Build and test on the simulator
        env:
          DESTINATION: ${{ steps.sim.outputs.destination }}
        run: |
          set -o pipefail
          xcodebuild test \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -destination "$DESTINATION" \
            -clonedSourcePackagesDirPath .spm \
            -skipMacroValidation \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            2>&1 | tee xcodebuild.log | xcbeautify --renderer github-actions

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcodebuild-log
          path: xcodebuild.log
          retention-days: 7
```

- [ ] **Step 4: `.github/workflows/testflight.yml`**
```yaml
name: TestFlight

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      signing:
        description: "Signing path (automatic = cloud-managed via the App Store Connect API key; manual = certificate and profiles from secrets)"
        type: choice
        options: [automatic, manual]
        default: automatic

concurrency:
  group: testflight
  cancel-in-progress: false

permissions:
  contents: read

env:
  XCODE_VERSION: "26.6"
  SIGNING: ${{ github.event.inputs.signing || 'automatic' }}

jobs:
  preflight:
    name: Check secrets
    runs-on: ubuntu-24.04
    outputs:
      configured: ${{ steps.check.outputs.configured }}
    steps:
      - id: check
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          missing=""
          [ -n "$ASC_KEY_ID" ] || missing="$missing ASC_KEY_ID"
          [ -n "$ASC_ISSUER_ID" ] || missing="$missing ASC_ISSUER_ID"
          [ -n "$ASC_KEY_P8" ] || missing="$missing ASC_KEY_P8"
          [ -n "$APPLE_TEAM_ID" ] || missing="$missing APPLE_TEAM_ID"
          if [ -n "$missing" ]; then
            echo "::notice::TestFlight upload skipped: missing repository secrets:$missing (see docs/release.md)"
            echo "configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

  upload:
    name: Archive and upload to TestFlight
    needs: preflight
    if: needs.preflight.outputs.configured == 'true'
    runs-on: macos-26
    timeout-minutes: 90
    env:
      REVOX_BUNDLE_PREFIX: ${{ vars.REVOX_BUNDLE_PREFIX || 'com.mchrisgm' }}
      BUILD_NUMBER: ${{ github.run_number }}
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: scripts/ci/select-xcode.sh "$XCODE_VERSION"

      - name: Install XcodeGen and xcbeautify
        run: brew install xcodegen xcbeautify

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Cache Swift package checkouts
        uses: actions/cache@v4
        with:
          path: .spm
          key: spm-${{ runner.os }}-${{ hashFiles('project.yml') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Resolve Swift packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -clonedSourcePackagesDirPath .spm

      - name: Pick simulator
        id: sim
        run: scripts/ci/pick-simulator.sh >> "$GITHUB_OUTPUT"

      - name: Run tests (a failure blocks the upload)
        env:
          DESTINATION: ${{ steps.sim.outputs.destination }}
        run: |
          set -o pipefail
          xcodebuild test \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -destination "$DESTINATION" \
            -clonedSourcePackagesDirPath .spm \
            -skipMacroValidation \
            -skipPackagePluginValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            2>&1 | tee test.log | xcbeautify --renderer github-actions

      - name: Write App Store Connect API key
        env:
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
        run: |
          mkdir -p ~/private_keys
          printf '%s' "$ASC_KEY_P8" | base64 --decode > ~/private_keys/AuthKey_"$ASC_KEY_ID".p8
          chmod 600 ~/private_keys/AuthKey_"$ASC_KEY_ID".p8
          echo "ASC_KEY_PATH=$HOME/private_keys/AuthKey_$ASC_KEY_ID.p8" >> "$GITHUB_ENV"

      - name: Import distribution certificate and profiles (manual signing only)
        if: env.SIGNING == 'manual'
        env:
          DIST_CERT_P12: ${{ secrets.DIST_CERT_P12 }}
          DIST_CERT_PASSWORD: ${{ secrets.DIST_CERT_PASSWORD }}
          PROFILE_APP: ${{ secrets.PROFILE_APP }}
          PROFILE_EXTENSION: ${{ secrets.PROFILE_EXTENSION }}
        run: |
          set -euo pipefail
          for v in DIST_CERT_P12 DIST_CERT_PASSWORD PROFILE_APP PROFILE_EXTENSION; do
            [ -n "${!v}" ] || { echo "::error::Manual signing needs secret $v"; exit 1; }
          done
          keychain="$RUNNER_TEMP/revox-signing.keychain-db"
          keychain_password=$(uuidgen)
          security create-keychain -p "$keychain_password" "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p "$keychain_password" "$keychain"
          printf '%s' "$DIST_CERT_P12" | base64 --decode > "$RUNNER_TEMP/dist.p12"
          security import "$RUNNER_TEMP/dist.p12" -P "$DIST_CERT_PASSWORD" -A -t cert -f pkcs12 -k "$keychain"
          security set-key-partition-list -S apple-tool:,apple: -k "$keychain_password" "$keychain"
          security list-keychains -d user -s "$keychain" login.keychain-db
          rm -f "$RUNNER_TEMP/dist.p12"

          install_profile() {  # $1 = secret value (base64), $2 = GITHUB_ENV key for the profile name
            local file="$RUNNER_TEMP/$2.mobileprovision"
            printf '%s' "$1" | base64 --decode > "$file"
            local plist uuid name
            plist=$(security cms -D -i "$file")
            uuid=$(printf '%s' "$plist" | plutil -extract UUID raw -o - -)
            name=$(printf '%s' "$plist" | plutil -extract Name raw -o - -)
            for dir in "$HOME/Library/MobileDevice/Provisioning Profiles" "$HOME/Library/Developer/Xcode/UserData/Provisioning Profiles"; do
              mkdir -p "$dir"
              cp "$file" "$dir/$uuid.mobileprovision"
            done
            echo "$2=$name" >> "$GITHUB_ENV"
            echo "Installed profile '$name' ($uuid)"
          }
          install_profile "$PROFILE_APP" REVOX_PROFILE_APP
          install_profile "$PROFILE_EXTENSION" REVOX_PROFILE_EXTENSION

      - name: Archive (automatic signing)
        if: env.SIGNING == 'automatic'
        run: |
          set -o pipefail
          xcodebuild archive \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath build/ReVoxMobile.xcarchive \
            -clonedSourcePackagesDirPath .spm \
            -skipMacroValidation \
            -skipPackagePluginValidation \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$ASC_KEY_PATH" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Automatic \
            REVOX_BUNDLE_PREFIX="$REVOX_BUNDLE_PREFIX" \
            CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            2>&1 | tee archive.log | xcbeautify --renderer github-actions

      - name: Archive (manual signing)
        if: env.SIGNING == 'manual'
        run: |
          set -o pipefail
          xcodebuild archive \
            -project ReVoxMobile.xcodeproj \
            -scheme ReVoxMobile \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath build/ReVoxMobile.xcarchive \
            -clonedSourcePackagesDirPath .spm \
            -skipMacroValidation \
            -skipPackagePluginValidation \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            REVOX_PROFILE_APP="$REVOX_PROFILE_APP" \
            REVOX_PROFILE_EXTENSION="$REVOX_PROFILE_EXTENSION" \
            REVOX_BUNDLE_PREFIX="$REVOX_BUNDLE_PREFIX" \
            CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            2>&1 | tee archive.log | xcbeautify --renderer github-actions

      - name: Write export options
        run: |
          set -euo pipefail
          out=ci/ExportOptions.generated.plist
          cp ci/ExportOptions.plist "$out"
          plutil -replace teamID -string "$APPLE_TEAM_ID" "$out"
          if [ "$SIGNING" = "manual" ]; then
            plutil -replace signingStyle -string manual "$out"
            plutil -replace signingCertificate -string "Apple Distribution" "$out"
            plutil -insert provisioningProfiles -dictionary "$out"
            plutil -insert "provisioningProfiles.${REVOX_BUNDLE_PREFIX}.revox" -string "$REVOX_PROFILE_APP" "$out"
            plutil -insert "provisioningProfiles.${REVOX_BUNDLE_PREFIX}.revox.broadcast" -string "$REVOX_PROFILE_EXTENSION" "$out"
          fi
          plutil -lint "$out"
          cat "$out"

      - name: Export IPA
        run: |
          set -o pipefail
          xcodebuild -exportArchive \
            -archivePath build/ReVoxMobile.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist ci/ExportOptions.generated.plist \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$ASC_KEY_PATH" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" \
            2>&1 | tee export.log | xcbeautify --renderer github-actions
          ls -la build/export

      - name: Upload to TestFlight
        run: |
          set -euo pipefail
          ipa=$(ls build/export/*.ipa | head -n 1)
          xcrun altool --upload-app -f "$ipa" -t ios \
            --apiKey "$ASC_KEY_ID" --apiIssuer "$ASC_ISSUER_ID"
          echo "Uploaded $ipa as build $BUILD_NUMBER. It appears in App Store Connect after processing (usually 5-15 minutes)."

      - name: Keep the IPA and logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: revox-mobile-build-${{ github.run_number }}
          path: |
            build/export/*.ipa
            test.log
            archive.log
            export.log
          retention-days: 14
          if-no-files-found: ignore

      - name: Remove signing material
        if: always()
        run: |
          rm -rf ~/private_keys ci/ExportOptions.generated.plist
          if [ "$SIGNING" = "manual" ]; then
            security delete-keychain "$RUNNER_TEMP/revox-signing.keychain-db" || true
          fi
```

Note on `plutil -insert "provisioningProfiles.<bundle id>"`: `plutil` uses `.` as a key-path separator, so a bundle id containing dots cannot be inserted that way. Instead, build the manual-signing dictionary with a small Python snippet using `plistlib` (Python 3 is on the runner): load the plist, set `signingStyle`, `signingCertificate`, `provisioningProfiles = {app_id: app_profile, ext_id: ext_profile}`, and write it back. Replace the four `plutil -insert/-replace` lines for the manual branch with that snippet, keep `plutil -lint`.

- [ ] **Step 5: Validate and commit**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); yaml.safe_load(open('.github/workflows/testflight.yml')); print('yaml ok')"
bash -n scripts/ci/select-xcode.sh && bash -n scripts/ci/pick-simulator.sh
chmod +x scripts/ci/*.sh
git add .github scripts/ci
git commit -m "ci: Linux package tests, iOS simulator build and test, TestFlight upload"   # plus the mandatory trailers
```

---

### Task 4: README, release guide, tester guide

**Files:**
- Create: `README.md`, `docs/release.md`, `docs/testing.md`

**Interfaces:**
- Consumes: names and secrets from the Global Constraints and Tasks 2 and 3.

- [ ] **Step 1: `README.md`** covering, in this order:
  1. One-paragraph description: ReVox Mobile is the iPhone version of [ReVox](https://github.com/mchrisgm/ReVox): it listens to audio (microphone, or other apps through a screen-broadcast extension), detects phrases with Silero VAD, translates them to English on-device with Whisper via WhisperKit, speaks the English with Kyutai pocket-tts (system voice until it is downloaded), and keeps a transcript. Fully offline after the one-time model download.
  2. Status: milestone table (0 bootstrap … 7 hardening) with the current one marked.
  3. Requirements: iPhone 12 or newer, iOS 17+, Wi-Fi for the first model download; developers need Xcode 26 and XcodeGen (`brew install xcodegen`), `ReVoxCore` also builds with the Swift 6.3 toolchain on Linux.
  4. Building: `xcodegen generate`, open `ReVoxMobile.xcodeproj`, scheme `ReVoxMobile`; `cd ReVoxCore && swift test` for the package; note the `.xcodeproj` is generated and never committed; note `REVOX_BUNDLE_PREFIX` in `project.yml` is the single place that defines the bundle ids and App Group.
  5. Architecture: the three targets plus the package, one sentence each; the data flow line `capture → Segmenter → segment queue (max 3) → WhisperKitTranslator → transcript + text queue → speaker → AudioPlayer → speaking edge → AudioSessionController`.
  6. Platform limitations (write these plainly): iOS cannot set another app's volume, so ducking uses `AVAudioSession` `.duckOthers` (system-determined amount) plus a voice-volume slider, and the Windows "ducked level" slider has no iOS equivalent; other-app capture requires the user to start a system broadcast (broadcast picker or Control Center); the broadcast extension has a 50 MB memory cap so all models live in the app; translation continues in the background thanks to the `audio` background mode.
  7. Delivery: CI on every push (`.github/workflows/ci.yml`), TestFlight on every push to `main` (`.github/workflows/testflight.yml`), links to `docs/release.md` and `docs/testing.md`.
  8. Licences: app MIT; WhisperKit MIT; pocket-tts Core ML weights CC-BY-4.0 (attribution to Kyutai); Silero VAD MIT.

- [ ] **Step 2: `docs/release.md`** covering:
  1. What the workflow does step by step (generate, test, archive, export, upload, build number = `github.run_number`, marketing version from `MARKETING_VERSION` in `project.yml`).
  2. One-time Apple setup as a numbered list (App Group, two App IDs with App Groups capability, Team ID, agreements, API key with App Manager role, app record with SKU `revox-mobile`).
  3. GitHub configuration: secrets table (`ASC_KEY_ID`, `ASC_ISSUER_ID`, `ASC_KEY_P8` with the exact command `base64 -i AuthKey_XXXX.p8 | pbcopy` on macOS, `APPLE_TEAM_ID`) and the optional repository variable `REVOX_BUNDLE_PREFIX` (needed only if the bundle ids differ from `com.mchrisgm.revox`).
  4. Path A, automatic (cloud-managed) signing: what `-allowProvisioningUpdates` with the API key does, and the two symptoms that mean it cannot handle the App Group entitlement on the runner (profile creation error mentioning the application-groups entitlement, or "No profiles for … were found").
  5. Path B, manual signing: how to create an Apple Distribution certificate, export the `.p12`, create two App Store provisioning profiles (app and extension), and add `DIST_CERT_P12`, `DIST_CERT_PASSWORD`, `PROFILE_APP`, `PROFILE_EXTENSION` as base64 secrets; run the workflow from the Actions tab with `signing = manual`.
  6. Behaviour before secrets exist: the `preflight` job reports a notice and the upload is skipped, so `main` stays green.
  7. After the upload: processing time, internal testing group, external (public link) group and Beta App Review.
  8. Troubleshooting: missing icon, `ITSAppUsesNonExemptEncryption`, privacy manifest warnings, agreement not accepted, `altool` deprecation note with the alternative of `-exportArchive` using `destination: upload`.

- [ ] **Step 3: `docs/testing.md`** for the tester: install TestFlight, accept the email invite (internal) or open the public link (external), first launch needs Wi-Fi to download the Whisper model (about 480 MB for the default `small`) and, optionally, the pocket-tts voice, microphone permission prompt, how to start translating from the microphone, what the broadcast picker does (starts a system screen broadcast that sends other apps' audio to ReVox; also available from Control Center's Screen Recording control), how to stop, where transcripts are, and how to send feedback (TestFlight screenshot feedback).

- [ ] **Step 4: Commit**
```bash
git add README.md docs/release.md docs/testing.md
git commit -m "docs: README, release guide and tester guide"   # plus the mandatory trailers
```
